#!/bin/bash

# shellcheck disable=SC2016
searchstring=$(printf 'SCRIPTVERSION="$(git describe --tags --always)"')
replstring=$(printf 'SCRIPTVERSION="%s"' "$(git describe --tags --always)")
sedcmd=$(printf "s/%s/%s/" "$searchstring" "$replstring")
sed -i "$sedcmd" ./gvm